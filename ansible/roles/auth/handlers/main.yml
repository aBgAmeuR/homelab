---
- name: Restart lldap
  ansible.builtin.systemd:
    name: lldap
    state: restarted
    daemon_reload: true

- name: Restart tinyauth
  ansible.builtin.systemd:
    name: tinyauth
    state: restarted
    daemon_reload: true
